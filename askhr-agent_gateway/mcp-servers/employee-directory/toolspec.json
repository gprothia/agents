{
  "tools": [
    {
      "name": "lookup_employee",
      "description": "Look up an employee by ID, name, or email. Returns directory profile.",
      "inputSchema": {
        "type": "object",
        "properties": { "query": { "type": "string" } },
        "required": ["query"]
      },
      "annotations": { "readOnlyHint": true }
    },
    {
      "name": "get_org_chart",
      "description": "Return the reporting chain and direct reports for an employee.",
      "inputSchema": {
        "type": "object",
        "properties": { "employee_id": { "type": "string" } },
        "required": ["employee_id"]
      },
      "annotations": { "readOnlyHint": true }
    },
    {
      "name": "search_hr_policies",
      "description": "Search company HR policy summaries (PTO, parental leave, remote work, expenses).",
      "inputSchema": {
        "type": "object",
        "properties": { "topic": { "type": "string" } },
        "required": ["topic"]
      },
      "annotations": { "readOnlyHint": true }
    }
  ]
}
