{
  "tools": [
    {
      "name": "get_payslip",
      "description": "Return the most recent payslip for an employee (contains payroll PII; governed by Model Armor).",
      "inputSchema": {
        "type": "object",
        "properties": { "employee_id": { "type": "string" } },
        "required": ["employee_id"]
      },
      "annotations": { "readOnlyHint": true }
    },
    {
      "name": "get_benefits_summary",
      "description": "Return benefits enrollment, 401k match, HSA balance and PTO balance.",
      "inputSchema": {
        "type": "object",
        "properties": { "employee_id": { "type": "string" } },
        "required": ["employee_id"]
      },
      "annotations": { "readOnlyHint": true }
    }
  ]
}
